/*

function inject() {
    if (!clientVisualizerInjected) {
        $("section.game").append("<div style=\"position: absolute; left: 80px; margin:10px; z-index: 100;\">this is an element injected into the client!</div>");
        clientVisualizerInjected = true;
    }
}

forceInjectClientVisualizer = function () {
    clientVisualizerInjected = false;
    inject();
};

inject();
*/